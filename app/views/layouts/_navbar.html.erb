<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
   <img src="<%= asset_path('teamup.png') %>" alt="App Icon" width="50" height="50" style="margin-right: 10px;" class="d-inline-block align-text-top">
    <%= link_to "NEO", @user.role == "student" ? project_hub_path : project_management_hub_path, class: "navbar-brand custom-brand" %>   
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
     <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
         <li class="nav-item">
          <% if @role == 'admin' %>
            <%= link_to "Manage Projects", project_management_hub_path, class: "nav-link" %>
          <% end %>
        </li>
         <li class="nav-item">
          <% if @role == 'admin' %>
            <%= link_to "Manage Students Score", edit_score_path, class: "nav-link" %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to "Calendar", calendar_view_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <% if @role == 'admin' %>
            <%= link_to "Events", events_path, class: "nav-link" %>
          <% end %>
        </li>
        <li class="nav-item">
          <% if @role == 'student' %>
            <%= link_to "Project Hub", project_hub_path, class: "nav-link" %>
          <% end %>
        </li>
      </ul>

      <span class="navbar-text d-none d-lg-inline" style="color: #F8d06D; margin: 0 10px;">|</span>

      <!-- User profile dropdown -->
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= @user.first_name %> <%= @user.last_name %>
            <img style="margin-left: 10px; border-radius:50%;" src="<%= @user.photo.present? ? "#{@user.photo}?sz=200" : asset_path('user icon.png') %>" alt="User Icon" width="30" height="30">
          </a>
          <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item" href="#">Profile</a></li>
            <li><a class="dropdown-item" href="#">Settings</a></li>
            <%if @role == 'admin'%>
                 <li><a class="dropdown-item" href="/import/data">Import Data</a></li>
            <%end%>       
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/logout">Logout</a></li>
          </ul>
        </li>
      </ul>
    </div>
  </nav>